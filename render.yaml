services:
  - type: web
    name: harvestify
    env: python
    plan: free
    python_version: 3.10
    build_command: pip install -r requirements.txt
    start_command: python app.py  # or however you start your app
